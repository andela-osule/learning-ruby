# nil and false are falsy,
# everything else is truthy.

if some_variable.is_a?(String)
    # do something
else
    # do something else
end

# unless := opposite of if

unless (1+1).equals?(2)
    puts "summed correctly" # this doesn't get printed.
end
